{% extends 'base.html.twig' %}

{% block main %}
<h1>Formulaire de contact</h1>
<div>
    {{ form_start(form) }}
    {{ form_row(form) }}
    <input type="submit" value="Envoyer">
    {{ form_end(form) }}
</div>
{% endblock %}
